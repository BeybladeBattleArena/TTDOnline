{
  "release": "2026-08-24-canonical-results-outcome-audio-v35-retry4",
  "includes": [
    "deploy the already-materialized canonical v35 result UI and exactly-once outcome lifecycle",
    "serve the registered MissionFail PCM WAV and wait for its actual ended event plus 1.2 seconds before results",
    "remove legacy result-summary and downstream reward-polish ownership",
    "retain EXP-orb and future avatar reward bonus notations in the native PIPS and EXP rows",
    "retain the portrait-safe Test Map switchback combat implementation",
    "register the canonical MissionFail WAV in the game asset manifest so release integrity validates the runtime asset"
  ]
}
