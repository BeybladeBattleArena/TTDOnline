{
  "release": "2026-08-23-production-recovery-v11",
  "includes": [
    "deploy the already-committed native-resolution designer art without any asset-import side effects",
    "deploy Firebase Hosting independently from Functions and Firestore",
    "redirect every known legacy low-resolution art URL to its canonical native-resolution master",
    "keep active game assets no-store so replaced art cannot be stranded behind stale browser caches",
    "validate the current serialized announcer behavior without stale pre-emption assertions",
    "verify the exact release commit from live build.json before declaring success",
    "smoke-test the live loading screens and critical item art at production-sized byte counts",
    "record a durable per-stage deployment diagnostic even when a release fails"
  ]
}
