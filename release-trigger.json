{
  "release": "2026-09-11-dark-monastery-real-repeat-reentry-v8",
  "includes": [
    "force known Dark Monastery starts back onto the original roaming stage object before V1 checks stage identity",
    "recover once through the preserved roaming starter if a later Adventure wrapper bypasses V1",
    "require live room canvases and joystick rather than a stale prior-run player object for runtime readiness",
    "regression-test a second run whose Adventure stage is rebuilt as a fresh object with its Dark Monastery marker stripped",
    "preserve START gameplay lock, pseudo-3D presentation, targetable monster proxies, positive damage numbers, and the half-size running effect"
  ]
}
