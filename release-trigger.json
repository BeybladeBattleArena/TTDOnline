{
  "release": "2026-08-24-original-announcer-transparent-items-v36",
  "includes": [
    "use the user-supplied MissionFail.mp3 byte-for-byte as the canonical FAIL announcer master",
    "allow MissionFail to reach its natural ended event in full before the post-FAIL result delay",
    "use the user-supplied CombatStart.mp3 byte-for-byte exactly when START! appears after 3 2 1",
    "materialize every active raster item PNG with real transparent backing while preserving native dimensions and dark item detail",
    "verify transparent item alpha on the live Firebase build",
    "retain canonical v35 result ownership, aligned item cards, server-authoritative progression, and portrait-safe combat"
  ]
}
