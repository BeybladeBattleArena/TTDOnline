{
  "name": "ttd-functions",
  "private": true,
  "main": "main-v5.js",
  "engines": { "node": "22" },
  "scripts": {
    "lint": "node --check index.js && node --check progression-v5.js && node --check main-v5.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
