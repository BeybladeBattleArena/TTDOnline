import { getApps, getApp } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import { getAuth } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import { getFunctions, httpsCallable } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-functions.js';
const frame=document.getElementById('gameFrame');let functions=null;
const ready=(async()=>{for(let i=0;i<500;i++){if(getApps().length){functions=getFunctions(getApp(),'us-central1');return;}await new Promise(r=>setTimeout(r,20));}throw new Error('Firebase did not initialize.');})();
window.addEventListener('message',async(event)=>{if(event.origin!==location.origin||event.source!==frame?.contentWindow)return;const m=event.data||{};if(m.type!=='ttd:v6-merge-request')return;try{await ready;if(!getAuth(getApp()).currentUser)throw new Error('Sign in again before merging.');const merge=await httpsCallable(functions,'mergeDice')({key:m.key,sourceId:m.sourceId,targetId:m.targetId});let data=merge.data||{};try{const s=(await httpsCallable(functions,'getSocialState')({})).data?.social;if(s?.self?.sharedDieId===m.sourceId||s?.self?.sharedDieId===m.targetId)await httpsCallable(functions,'setSharedDie')({instanceId:m.targetId});}catch(socialErr){console.warn('Shared Die pointer could not be refreshed after merge.',socialErr);}frame.contentWindow.postMessage({type:'ttd:v6-merge-result',requestId:m.requestId,...data},location.origin);}catch(err){frame.contentWindow.postMessage({type:'ttd:v6-merge-error',requestId:m.requestId,message:err?.message?.replace(/^FirebaseError:\s*/i,'')||'The server rejected that Class merge.'},location.origin);}},true);
