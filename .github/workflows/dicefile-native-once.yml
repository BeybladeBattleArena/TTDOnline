name: One-time Native Dicefile Handoff

on:
  push:
    branches: [cleanup/lossless-infrastructure]

permissions:
  contents: write

concurrency:
  group: ttd-dicefile-native-once
  cancel-in-progress: false

jobs:
  materialize:
    if: startsWith(github.event.head_commit.message, '[dicefile-native-v1]')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Materialize native dicefile data handoff
        run: node scripts/materialize-native-dicefile-v1.mjs

      - name: Constrain migration scope
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t changed < <(git diff --name-only | sort)
          printf '%s\n' "${changed[@]}"
          [[ "${#changed[@]}" -eq 2 ]]
          [[ "${changed[0]}" == 'online/game-loader.js' ]]
          [[ "${changed[1]}" == 'random-dice-game-33.html' ]]

      - name: Install validation toolchain
        run: |
          npm install
          npm --prefix functions install

      - name: Validate native dicefile runtime
        run: npm run check

      - name: Recheck immutable assets
        run: npm run check:immutable-assets

      - name: Commit validated native dicefile handoff
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'ttd-cleanup-bot'
          git config user.email 'ttd-cleanup-bot@users.noreply.github.com'
          git add online/game-loader.js random-dice-game-33.html
          git diff --cached --quiet && { echo 'No migration changes to commit.'; exit 1; }
          git commit -m 'infra: make dicefile runtime handoff data-only [skip dicefile-native-v1]'
          git push origin HEAD:cleanup/lossless-infrastructure
